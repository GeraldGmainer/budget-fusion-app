import 'package:flutter/material.dart';

class QueueTab extends StatelessWidget {
  const QueueTab();

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Queue debug goes here'));
  }
}
